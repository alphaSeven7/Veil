# （占位任务）Veil 初始化 — 待首个真实任务替换

> **文档定位**：跨会话开发计划。让任何新对话读这份文档 + AGENTS.md + 关键代码位置即可独立推进任务。
>
> **目标读者**：接手这个任务的 Codex agent（或人）。
>
> **创建日期**：2026-09-12
> **最后更新**：2026-09-12
> **当前阶段**：Phase 0 — 项目骨架（bootstrap 完成）
>
> **Obsidian 路径**：`~/Documents/Obisidian/LocalProject/Project/nlviews 任务/20260912_nlviews.md`

---

## 0. 30 秒摘要（TL;DR）

| 维度 | 现状 | 目标 |
|------|------|------|
| 问题 | 等待首个真实任务开始时填写 | 等待首个真实任务开始时填写 |
| 范围 | 等待首个真实任务开始时填写 | 等待首个真实任务开始时填写 |
| 工作量 | 等待首个真实任务开始时填写 | 等待首个真实任务开始时填写 |
| 风险 | 等待首个真实任务开始时填写 | 等待首个真实任务开始时填写 |

**TL;DR 文字版**（一两句话，能让任何 agent 3 秒内理解任务核心）：

> 等待首个真实任务开始时填写

---

## 1. 任务交接状态（每次新会话开始必读）

### 1.1 进度总览

| Phase | 状态 | 完成度 | 最近更新 | 备注 |
|-------|------|--------|----------|------|
| Phase 1: 等待首个真实任务开始时填写 | ✅ 完成 | 100% | 2026-09-12 | — |
| Phase 2: 等待首个真实任务开始时填写 | 🟡 进行中 | 60% | 2026-09-12 | 阻塞：等待首个真实任务开始时填写 |
| Phase 3: 等待首个真实任务开始时填写 | ⏳ 待开始 | 0% | — | 依赖 Phase 2 |
| Phase 4: 等待首个真实任务开始时填写 | ⏳ 待开始 | 0% | — | — |

图例：✅ 完成 / 🟡 进行中（具体 %）/ ⛔ 阻塞 / ⏳ 待开始 / 🚫 取消

### 1.2 最近一次会话信息

- **会话日期**：2026-09-12
- **完成项**：
  - {本会话做了什么 - 1}
  - {本会话做了什么 - 2}
- **下一步（下次会话从这里继续）**：
  1. {具体可执行任务 1}
  2. {具体可执行任务 2}
- **阻塞 / 风险**：
  - {问题 1 — 等待谁 / 何时解决}
- **备份位置**：`~/Documents/Codex/nlviews-backups/20260912_014112-bootstrap-staged/`
- **相关 PR / 分支**：{branch / PR URL（如有）}

### 1.3 关键决策记录（避免下次重复讨论）

| 日期 | 决策 | 原因 | 决策人 |
|------|------|------|--------|
| 2026-09-12 | {决策 1} | {原因 / 权衡} | {Codex / user} |
| 2026-09-12 | {决策 2} | {原因 / 权衡} | {Codex / user} |

### 1.4 已知未解决问题

| 问题 | 状态 | 影响 | 备注 |
|------|------|------|------|
| 等待首个真实任务开始时填写 | 🔴 开放 / 🟡 搁置 / ✅ 已解 | 等待首个真实任务开始时填写 | — |

### 1.5 后续会话 check-list

下次会话开始时，先做这三件事再读其它章节：

- [ ] 读 §1.2 "最近一次会话信息" 和 §1.4 "已知未解决问题"
- [ ] 跑 {快速健康检查命令}（如 `git status` + `curl /health`）
- [ ] 确认 {继续 Phase X.Y 的具体子任务}

---

## 2. 项目背景

### 2.1 项目本体

- **项目名**：nlviews
- **路径**：`/Users/alphaseven/Documents/Codex/nlviews`
- **技术栈**：{framework + language + DB + ...}
- **一句话描述**：{项目做什么}

### 2.2 相关历史（必读）

按时间**倒序**列出与本次任务直接相关的已有修改或讨论：

| 日期 | 改动 / 讨论 | 文件 / 链接 | 与本次任务的关系 |
|------|------------|------------|----------------|
| 2026-09-12 | {改动摘要} | `path/to/file.py:L123` | {原因} |
| 2026-09-12 | {改动摘要} | `commit abc123` | {原因} |

### 2.3 已有设计文档（如有）

- `docs/{DESIGN_DOC}.md` — {一句话说明}
- {其它参考链接}

---

## 3. 本次任务的核心问题（"为什么"）

### 3.1 业务现象

{用户/测试/生产日志看到的现象 — 包括具体的错误信息、截图引用、复现步骤}

### 3.2 完整根因链

按调用栈从现象到根因，每一步都要可验证：

1. **症状**：{现象 1}
2. **第一层根因**：{原因 1}
3. **第二层根因**：{原因 2}
4. **根本根因**：{最深层原因}

---

## 4. 当前架构（必须理解的已有结构）

### 4.1 核心抽象 / 接口

```python
# 关键 dataclass / Protocol / 函数签名
@decorator
class XxxAdapter:
    def method_a(self) -> ReturnType: ...
```

### 4.2 已有文件

| 文件 | 行数 | 角色 | 关键位置 |
|------|------|------|----------|
| `path/to/file.py` | {N} | {职责} | `L123-200` {说明} |

### 4.3 调用关系图

```
[组件 A] -- 调用 --> [组件 B] -- 继承 --> [基类]
                                \
                                 --> [辅助 C]
```

---

## 5. 实施步骤（"怎么做"）

### 5.1 总体顺序

| 步骤 | 内容 | 预估 | 依赖 |
|------|------|------|------|
| 1 | {step 1} | {time} | — |
| 2 | {step 2} | {time} | step 1 |
| 3 | {step 3} | {time} | — |

**总预计**：{time}

### 5.2 Phase 1: 等待首个真实任务开始时填写

#### 设计决策

| 问题 | 决策 | 理由 |
|------|------|------|
| {决策点 1} | {怎么决定} | {为什么} |

#### 任务

- [ ] {task 1 — 具体可验证}
- [ ] {task 2 — 具体可验证}
- [ ] 跑测试：{测试命令 + 预期结果}

#### 关键代码

```python
# path/to/file.py:L123-L180
{snippet}
```

### 5.3 Phase 2: 等待首个真实任务开始时填写

#### 任务

- [ ] {task 1}
- [ ] {task 2}

#### 关键代码

```python
{snippet}
```

### 5.4 Phase 3: 等待首个真实任务开始时填写
...

---

## 6. 预期效果（"达到什么"）

### 6.1 行为变化（Before / After）

| 场景 | Before | After |
|------|--------|-------|
| {场景 1} | {旧行为} | {新行为} |
| {场景 2} | {旧行为} | {新行为} |

### 6.2 量化指标

| 指标 | Before | After |
|------|--------|-------|
| {指标 1，如测试覆盖率} | {值} | {目标值} |
| {指标 2，如 7000 表采集耗时} | {值} | {目标值} |
| {指标 3，如错误率} | {值} | {目标值} |

---

## 7. 验证方法（"怎么验证"）

### 7.1 单元测试

- **新增测试**：`tests/test_{feature}.py`（约 {N} 个用例）
- **预期**：{N} passed
- **命令**：

```bash
{PYTHONPATH=. ./.venv-test/bin/pytest -q tests/test_{feature}.py}
```

### 7.2 集成测试 + 回归

- **既有测试套件**：{既有套件列表}
- **预期**：所有 fix/新测试 PASS，pre-existing 失败数不变（{N} 个）
- **命令**：

```bash
{PYTHONPATH=. ./.venv-test/bin/pytest -q}
```

### 7.3 部署后验证

按 `DEPLOY.md` 流程：

```bash
# 1. 打包
{PACKAGE_CMD}
# 产出：dist/nlviews_20260912_0141.tar.gz

# 2. 上传部署
./build.sh        # 本地编译并打包 .app + .pkg

# 3. 重启
# （由 deploy.sh 自动）

# 4. 健康检查
curl -s -o /dev/null -w "%{http_code}" http://localhost:54345/health
# 预期：200

# 5. 触发业务测试
# {业务测试命令 / 手动操作步骤}

# 6. 验证日志
ssh alphaseven@localhost "tail -100 /Users/alphaseven/Library/Application Support/Veil/logs/app.log | grep -i error"
# 预期：{空 / 无 X 错误}
```

### 7.4 浏览器冒烟（如适用）

- {页面 1}：{期望看到什么}
- {页面 2}：{期望看到什么}

---

## 8. AGENTS.md 约束（必读，违反会破坏其它 session 工作）

本次任务受 AGENTS.md 以下规则强约束：

| 规则编号 | 内容 | 与本次任务的关系 |
|---------|------|----------------|
| 1 | 禁止删除文件 | {本次涉及} |
| 2 | 修改前先备份 | 备份到 `~/Documents/Codex/nlviews-backups/20260912_014112-bootstrap/` |
| 4 | 优先用 `dist/*.tar.gz` 回退 | {本次涉及} |
| 5 | 本地校验 | {校验命令} |
| 6 | 部署前 MD5 一致 | {MD5 校验步骤} |
| 7 | 部署后健康验证 | {健康检查步骤} |
| 8 | 不能 `git rm` 已 tracked 文件 | {本次涉及} |
| 10 | 完成代码变更后更新文档 / 图谱 | {本次涉及：CHANGELOG + 本文档 + graph} |

**强约束提醒**：本次任务**不会**改动 {out of scope 清单}，避免影响其它 session。

---

## 9. 关键代码位置速查表

| 主题 | 文件:行 | 说明 |
|------|---------|------|
| {topic 1} | `path/to/file.py:L123` | {说明} |
| {topic 2} | `path/to/file.py:L456-500` | {说明} |
| {相关测试} | `tests/test_x.py::TestCase` | {mock 模式参考} |
| {相关文档} | `docs/DEVELOPMENT_PLAN.md.md` | {一句话} |

---

## 10. 故障排查指南

### 10.1 {symptom 1}

- **表现**：{user 看到什么}
- **检查**：
  ```bash
  {诊断命令}
  ```
- **根因**：{why it happens}
- **修复**：{how to fix}

### 10.2 {symptom 2}

- **表现**：{user 看到什么}
- **检查**：{诊断命令}
- **根因**：{why}
- **修复**：{how}

### 10.3 {symptom 3 — 已知陷阱}
{详细描述 — 这部分是经验沉淀，必读}

---

## 11. 后续可选改进（不在本次范围）

- [ ] {idea 1 — 简短描述}
- [ ] {idea 2}
- [ ] {idea 3}

---

## 附录 A：TDD 工作流（必须遵循）

1. **写失败测试** — 先写一个能体现需求的测试，描述期望行为
2. **跑测试确认失败** — 确认测试因正确原因失败（不是 import error / typo）
3. **写最小实现** — 让测试通过的最少代码
4. **跑测试确认通过** — 全套测试不应有回归
5. **重构** — 改进命名 / 拆分函数 / 提取常量，保持测试全过

> 每完成一个 Phase 都要走一遍 TDD 五步。

---

## 附录 B：参考的现有测试 / 实现

- `tests/test_{existing_a}.py` — 模式 A 的参考实现（mock {x}）
- `tests/test_{existing_b}.py` — 模式 B 的参考实现（mock {y}）
- `services/{existing_module}.py` — {现有可复用函数}

---

## 附录 C：变更日志（本次任务专用）

| 日期 | Phase | 变更 | 备份 |
|------|-------|------|------|
| 2026-09-12 | Phase 1 | {一句话} | `~/Documents/...` |
| 2026-09-12 | Phase 2 | {一句话} | `~/Documents/...` |

---

## 占位符清单

| 占位符 | 含义 | 示例 |
|--------|------|------|
| `（占位任务）Veil 初始化 — 待首个真实任务替换` | 任务标题 | `4 个 Collector 统一重构（MYSQL / ORACLE / DM）` |
| `nlviews` | 项目短名 | `db-aiops` |
| `/Users/alphaseven/Documents/Codex/nlviews` | 本地项目路径 | `/Users/alphaseven/Documents/Codex/db-aiops` |
| `20260912_nlviews` | Obsidian 文件名 | `20260911_4collector_refactor` |
| `localhost` / `alphaseven` / `54345` / `/health` / `/Users/alphaseven/Library/Application Support/Veil/logs/app.log` | 见 DEPLOY.template.md | — |
| `{PACKAGE_CMD}` / `./build.sh        # 本地编译并打包 .app + .pkg` | 见 DEPLOY.template.md | — |

---

## 模板元信息

- **模板版本**：0.1.0（2026-09-12）
- **基于**：`db-aiops/docs/DEVELOPMENT_PLAN_20260911_4collector_refactor.md`（862 行）
- **核心设计**：把"会话交接"提到 §1，作为新会话第一动作；其它章节按"Why / How / Goal / Verify / Risks"经典骨架组织。
