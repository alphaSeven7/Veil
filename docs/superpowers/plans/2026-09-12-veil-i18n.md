# Veil · 国际化 UI（zh-CN / en-US） Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Veil Web UI（zh-CN 单语）改造为 zh-CN + en-US 双语界面，零外部依赖，运行时可切换并持久化。

**Architecture:**
- 自研轻量运行时 `app/Web/i18n.js`（~200 行原生 ES2017）
- 字典文件 `app/Web/locales/zh-CN.json` + `en-US.json`
- HTML 通过 `data-i18n` 属性声明；JS 通过 `i18n.t(...)` 调用
- Swift 侧追加 2 个 API 端点 + StaticFiles 支持 `locales/` 子目录
- 切换语言走 `saveSettings` → JS 注入 `setLocale + applyToDOM`（不刷新页面）

**Tech Stack:** Swift 6.1 / AppKit / WebKit · 原生 JS ES2017 · JSON · ICU MessageFormat 子集

## Global Constraints

来自 `~/Documents/Obisidian/LocalProject/Project/nlviews 任务/20260912_nlviews_i18n.md` §5.2：

- **零 JS 运行时依赖**：禁止使用 i18next / @formatjs 等第三方库
- **备份强制**：动手前必须 `cp -R` 到 `~/Documents/Codex/nlviews-backups/20260912_HHMM-i18n/`
- **不删文件**：AGENTS.md 规则 1 — 任何"清理"必须先备份再修改
- **最小 Swift 侵入**：Models.swift `AppSettings.locale` 已存在，禁止新增重复字段
- **翻译工作流**：AI 生成 + 人工抽检 20%
- **zh-CN 必须始终可用**：缺失 en-US 键必须降级到 zh-CN（不能直接显示 key 名）
- **i18n.js API 稳定性**：`init / t / setLocale / applyToDOM / formatDate / formatNumber / formatRelative` — 任何后续任务不得改名或删函数
- **JS 注释/正则中的中文保留**：grep 验证只看 JS 字符串字面量；正则、注释、URL 中的中文字符不动
- **locale 字典加载路径**：`/__veil/locales/<lang>.json`，由 LocalAPI 提供
- **运行时注入点**：`window.__veilLocale` 在 AppDelegate 启动 WKWebView 后注入
- **持久化**：写到 `~/Library/Application Support/Veil/vault.json` 的 `settings.locale` 字段（已有）

---

## File Structure

**新建（4 个）：**
- `app/Web/i18n.js` — 运行时（~200 行）
- `app/Web/locales/zh-CN.json` — 简体中文字典
- `app/Web/locales/en-US.json` — 英文字典
- `docs/superpowers/specs/.gitkeep` — 占位目录（空）

**修改（5 个）：**
- `app/Web/index.html` — 引入 i18n.js；DOMContentLoaded 调 init；可见文本加 data-i18n
- `app/Web/detect.html` — 35+ 项测试名走 i18n
- `app/Web/app.js` — 所有用户可见字符串改 `i18n.t(...)`
- `app/Sources/StaticFiles.swift` — 支持 `locales/` 子目录服务
- `app/Sources/LocalAPI.swift` — 新增 `GET /__veil/locale` + `GET /__veil/locales/<lang>` 端点
- `app/Sources/AppDelegate.swift` — 启动后注入 `window.__veilLocale`；监听 settings 变化触发 JS 重渲染

**不变：** Models.swift（locale 字段已存在）、其它 Swift 文件、pkg/、tools/

---

## Task Decomposition

### Task 1: 备份当前代码

**Files:**
- Read: `app/Web/`、`app/Sources/StaticFiles.swift`、`app/Sources/LocalAPI.swift`、`app/Sources/AppDelegate.swift`、`app/Sources/Models.swift`
- Write: `~/Documents/Codex/nlviews-backups/20260912_HHMM-i18n/`

**Interfaces:** 无（pure 文件操作）

- [ ] **Step 1: 建备份目录**

```bash
TS=$(date +%Y%m%d_%H%M)
SNAP=~/Documents/Codex/nlviews-backups/${TS}-i18n
mkdir -p "$SNAP"
echo "$SNAP" > /tmp/veil-i18n-snapshot-path
echo "SNAP=$SNAP"
```

- [ ] **Step 2: 复制要改的文件到备份**

```bash
SNAP=$(cat /tmp/veil-i18n-snapshot-path)
cp -R /Users/alphaseven/Documents/Codex/nlviews/app/Web "$SNAP/app-Web-before"
cp /Users/alphaseven/Documents/Codex/nlviews/app/Sources/StaticFiles.swift "$SNAP/"
cp /Users/alphaseven/Documents/Codex/nlviews/app/Sources/LocalAPI.swift "$SNAP/"
cp /Users/alphaseven/Documents/Codex/nlviews/app/Sources/AppDelegate.swift "$SNAP/"
cp /Users/alphaseven/Documents/Codex/nlviews/app/Sources/Models.swift "$SNAP/"
```

- [ ] **Step 3: 验证备份完整性**

```bash
SNAP=$(cat /tmp/veil-i18n-snapshot-path)
diff -r "$SNAP/app-Web-before" /Users/alphaseven/Documents/Codex/nlviews/app/Web && echo "Web OK"
md5sum /Users/alphaseven/Documents/Codex/nlviews/app/Sources/*.swift | diff - <(md5sum "$SNAP"/*.swift) && echo "Swift OK"
```

Expected: `Web OK` + `Swift OK` 输出。两条都打才算备份成功。

- [ ] **Step 4: 提交当前 git 状态**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
git status --short
# 期望：working tree clean（之前已 commit & push 到 GitHub）
```

Expected: 无输出（无未提交修改）。如有不一致输出，停下来排查。

---

### Task 2: 写 i18n.js 运行时骨架（init / t / setLocale / applyToDOM）

**Files:**
- Create: `app/Web/i18n.js`

**Interfaces (this task defines):**
- `i18n.init(opts)` → Promise<void>，opts = {locale, fallback, localesUrl, dict?}
- `i18n.t(key, params?)` → string
- `i18n.setLocale(locale)` → Promise<void>
- `i18n.getLocale()` → string
- `i18n.applyToDOM(root?)` → void
- `i18n.formatDate(d, opts?)` → string
- `i18n.formatNumber(n, opts?)` → string
- `i18n.formatRelative(seconds)` → string
- `i18n.listMissing()` → string[]

- [ ] **Step 1: 写 i18n.js 头部注释与状态**

创建 `app/Web/i18n.js`，写入：

```javascript
/* Veil · 轻量 i18n 运行时（零依赖，原生 ES2017+）
 * 公共 API: init / t / setLocale / getLocale / applyToDOM /
 *          formatDate / formatNumber / formatRelative / listMissing
 */
(function (root) {
  'use strict';

  const state = {
    locale: 'zh-CN',
    fallback: 'zh-CN',
    dict: { 'zh-CN': {}, 'en-US': {} },
    loaded: { 'zh-CN': true, 'en-US': false },
    localesUrl: '/__veil/locales',
    missing: new Set(),
    ready: null, // Promise
  };

  function warn(msg) {
    if (typeof console !== 'undefined') console.warn('[i18n]', msg);
  }

  // 缺失键优雅降级：返回 key 名 + 记录
  function miss(key) {
    state.missing.add(key);
    return key;
  }

  // 加载指定语言字典
  async function loadLocale(locale) {
    if (state.loaded[locale]) return state.dict[locale];
    try {
      const res = await fetch(state.localesUrl + '/' + locale + '.json');
      if (!res.ok) throw new Error('HTTP ' + res.status);
      state.dict[locale] = await res.json();
      state.loaded[locale] = true;
      return state.dict[locale];
    } catch (e) {
      warn('加载 ' + locale + ' 失败：' + e.message);
      return {};
    }
  }

  // 按点分路径取字典值
  function lookup(dict, key) {
    if (!dict) return undefined;
    const parts = key.split('.');
    let cur = dict;
    for (const p of parts) {
      if (cur && typeof cur === 'object' && p in cur) cur = cur[p];
      else return undefined;
    }
    return (typeof cur === 'string') ? cur : undefined;
  }

  // 简易 ICU 子集（插值 + 复数 + select）
  function format(template, params) {
    if (!params) return template;
    return template.replace(
      /\{\{\s*([a-zA-Z_][\w]*)\s*\}\}|\{\s*([a-zA-Z_][\w]*)\s*(?:,\s*(plural|select)\s*,)?\s*([^}]*)\}/g,
      (m, v1, v2, kind, body) => {
        if (v1) return String(params[v1] ?? '');
        const name = v2;
        const v = params[name];
        if (kind === 'plural') {
          const n = Number(v);
          const branches = parseBranches(body);
          const key = (n === 1 && 'one' in branches) ? 'one' : 'other';
          const tpl = branches[key] ?? branches.other ?? '';
          return tpl.replace(/#/g, String(n));
        }
        if (kind === 'select') {
          const branches = parseBranches(body);
          return branches[String(v)] ?? branches.other ?? '';
        }
        return String(v ?? '');
      }
    );
  }

  function parseBranches(body) {
    const out = {};
    const re = /(\w+)\s*\{([^}]*)\}/g;
    let m;
    while ((m = re.exec(body)) !== null) out[m[1]] = m[2];
    return out;
  }

  // 公共 API
  const i18n = {
    async init(opts) {
      opts = opts || {};
      state.locale = opts.locale || (root.__veilLocale || 'zh-CN');
      state.fallback = opts.fallback || 'zh-CN';
      state.localesUrl = opts.localesUrl || '/__veil/locales';
      if (opts.dict) {
        for (const k of Object.keys(opts.dict)) {
          state.dict[k] = opts.dict[k];
          state.loaded[k] = true;
        }
      }
      state.ready = Promise.all([loadLocale(state.locale), loadLocale(state.fallback)]);
      await state.ready;
      return state.ready;
    },

    t(key, params) {
      let v = lookup(state.dict[state.locale], key);
      if (v === undefined && state.locale !== state.fallback) {
        v = lookup(state.dict[state.fallback], key);
      }
      if (v === undefined) return miss(key);
      return format(v, params);
    },

    async setLocale(locale) {
      await loadLocale(locale);
      state.locale = locale;
    },

    getLocale() { return state.locale; },

    applyToDOM(rootEl) {
      const root = rootEl || document;
      // 文本节点
      root.querySelectorAll('[data-i18n]').forEach((el) => {
        const key = el.getAttribute('data-i18n');
        const txt = i18n.t(key);
        if (el.textContent !== txt) el.textContent = txt;
      });
      // 多属性：data-i18n-attr="title:foo;aria-label:bar"
      root.querySelectorAll('[data-i18n-attr]').forEach((el) => {
        const spec = el.getAttribute('data-i18n-attr') || '';
        spec.split(';').forEach((pair) => {
          const [attr, key] = pair.split(':').map((s) => s.trim());
          if (!attr || !key) return;
          const txt = i18n.t(key);
          if (el.getAttribute(attr) !== txt) el.setAttribute(attr, txt);
        });
      });
      // 单属性快捷：data-i18n-placeholder / data-i18n-title / data-i18n-html
      ['placeholder', 'title', 'aria-label'].forEach((attr) => {
        const dn = 'data-i18n-' + attr;
        root.querySelectorAll('[' + dn + ']').forEach((el) => {
          const key = el.getAttribute(dn);
          const txt = i18n.t(key);
          if (el.getAttribute(attr) !== txt) el.setAttribute(attr, txt);
        });
      });
      // innerHTML（谨慎使用）
      root.querySelectorAll('[data-i18n-html]').forEach((el) => {
        const key = el.getAttribute('data-i18n-html');
        el.innerHTML = i18n.t(key);
      });
    },

    formatDate(d, opts) {
      try {
        return new Intl.DateTimeFormat(state.locale, opts || {
          year: 'numeric', month: '2-digit', day: '2-digit',
          hour: '2-digit', minute: '2-digit'
        }).format(d);
      } catch (e) { return String(d); }
    },

    formatNumber(n, opts) {
      try {
        return new Intl.NumberFormat(state.locale, opts).format(n);
      } catch (e) { return String(n); }
    },

    formatRelative(seconds) {
      try {
        const rtf = new Intl.RelativeTimeFormat(state.locale, { numeric: 'auto' });
        if (Math.abs(seconds) < 60) return rtf.format(-Math.round(seconds), 'second');
        if (Math.abs(seconds) < 3600) return rtf.format(-Math.round(seconds / 60), 'minute');
        if (Math.abs(seconds) < 86400) return rtf.format(-Math.round(seconds / 3600), 'hour');
        return rtf.format(-Math.round(seconds / 86400), 'day');
      } catch (e) { return String(seconds); }
    },

    listMissing() { return Array.from(state.missing); },
  };

  root.i18n = i18n;
})(typeof window !== 'undefined' ? window : globalThis);
```

- [ ] **Step 2: 语法验证**

```bash
node --check /Users/alphaseven/Documents/Codex/nlviews/app/Web/i18n.js && echo "SYNTAX OK"
```

Expected: `SYNTAX OK`

- [ ] **Step 3: 浏览器控制台冒烟（用 Python 启静态服务）**

```bash
# 后台启动静态服务
python3 -m http.server 8765 --directory /Users/alphaseven/Documents/Codex/nlviews/app/Web > /tmp/i18n-http.log 2>&1 &
HTTP_PID=$!
sleep 1
# 测试 i18n.js 能被 fetch
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8765/i18n.js
kill $HTTP_PID 2>/dev/null
```

Expected: `200`

- [ ] **Step 4: 提交 i18n.js**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
git add app/Web/i18n.js
git commit -m "feat(i18n): 自研轻量运行时骨架 (init/t/setLocale/applyToDOM/format*)"
```

Expected: commit hash 输出。

---

### Task 3: 写 zh-CN.json 基线字典（提取现有中文）

**Files:**
- Create: `app/Web/locales/zh-CN.json`
- Create: `app/Web/locales/.gitkeep`（保留空目录提交）

**Interfaces (this task defines):**
- 字典 schema：`_meta` + 业务键树（nav.* / toast.* / dialog.* / settings.* / platform.* / detect.*）

- [ ] **Step 1: 扫描现有硬编码中文**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
echo "=== app.js 中的中文字符串（去重） ==="
grep -oE "['\"\`][^'\"\`]*[\xe4-\xe9][^'\"\`]*['\"\`]" app/Web/app.js | \
  sed -E "s/^['\"\`]//;s/['\"\`]?$//" | sort -u > /tmp/zh-strings-appjs.txt
wc -l /tmp/zh-strings-appjs.txt
echo "=== index.html 中的中文 ==="
grep -oE ">[^<]*[\xe4-\xe9][^<]*<" app/Web/index.html | \
  sed -E "s/[<>]//g;s/^ +//;s/ +$//" | sort -u > /tmp/zh-strings-index.txt
wc -l /tmp/zh-strings-index.txt
echo "=== detect.html 中的中文 ==="
grep -oE ">[^<]*[\xe4-\xe9][^<]*<" app/Web/detect.html | \
  sed -E "s/[<>]//g;s/^ +//;s/ +$//" | sort -u > /tmp/zh-strings-detect.txt
wc -l /tmp/zh-strings-detect.txt
```

Expected: 三个数字输出（app.js 几百、index.html ~30、detect.html ~50）。记下来作为字典规模参考。

- [ ] **Step 2: 手工审阅去重列表**

打开三个 .txt 文件，人工合并同义字符串，按以下结构分组：

```
nav.*         导航相关（窗口 / 运行中 / 分组 / 指纹模板 / 设置 / API 等）
toast.*       提示消息（已保存 / 已删除 / 打开失败 / 代理检测成功等）
dialog.*      对话框标题 / 确认消息
button.*      按钮文字（新建 / 编辑 / 删除 / 导入 / 导出等）
settings.*    设置页字段名
platform.*    平台枚举（Windows / macOS / Linux / Android）
proxy.*       代理类型 / 状态（HTTP / HTTPS / SOCKS5 / 不使用 / 跟随系统）
group.*       分组相关
fingerprint.*  指纹字段名（UA / Canvas / WebGL 等）
detect.*      detect.html 35+ 项测试名
common.*      通用词（确定 / 取消 / 搜索 / 关闭 / 刷新 / 全选等）
onboarding.*  首次启动欢迎语（可选）
error.*       错误消息
```

把分组结果写到一个新文件 `/tmp/zh-dict-plan.json` 供 Step 3 使用。

- [ ] **Step 3: 写 zh-CN.json（基线字典）**

创建 `app/Web/locales/zh-CN.json`：

```json
{
  "_meta": {
    "name": "简体中文",
    "code": "zh-CN",
    "pluralRule": "zh",
    "direction": "ltr"
  },
  "nav": {
    "profiles": "窗口",
    "running": "运行中",
    "groups": "分组",
    "templates": "指纹模板",
    "settings": "设置",
    "detect": "指纹检测",
    "api": "本地 API",
    "searchPlaceholder": "搜索窗口 / 备注 / 序号 / 代理…",
    "count": "{n} 个"
  },
  "common": {
    "ok": "确定",
    "cancel": "取消",
    "save": "保存",
    "saved": "已保存",
    "delete": "删除",
    "edit": "编辑",
    "create": "新建",
    "refresh": "刷新",
    "search": "搜索",
    "close": "关闭",
    "yes": "是",
    "no": "否",
    "all": "全部",
    "none": "无",
    "copy": "复制",
    "duplicate": "克隆",
    "import": "导入",
    "export": "导出",
    "yesDelete": "确定删除",
    "untitled": "未命名"
  },
  "toast": {
    "saved": "已保存",
    "deleted": "已删除",
    "created": "已创建",
    "openFailed": "打开失败：{error}",
    "deleteFailed": "删除失败：{error}",
    "imported": "已导入 {n} 个窗口",
    "exported": "已导出 {n} 个窗口",
    "copied": "已复制",
    "duplicated": "已克隆",
    "savedAndReload": "已保存，请刷新页面查看效果",
    "noChanges": "未做修改",
    "operationFailed": "操作失败：{error}"
  },
  "dialog": {
    "newProfile": "新建窗口",
    "editProfile": "编辑窗口",
    "deleteConfirm": "确定要删除「{name}」吗？",
    "deleteCountConfirm": "确定要删除 {n} 个窗口吗？",
    "deleteGroupConfirm": "确定要删除分组「{name}」吗？组内窗口将移到默认分组。",
    "unsavedChanges": "有未保存的修改，确定要离开吗？"
  },
  "button": {
    "newProfile": "新建窗口",
    "newGroup": "新建分组",
    "duplicateProfile": "克隆窗口",
    "delete": "删除选中",
    "enable": "启用",
    "disable": "停用",
    "open": "打开",
    "close": "关闭",
    "refresh": "刷新",
    "batchImport": "批量导入",
    "batchExport": "批量导出",
    "generateFingerprint": "生成随机指纹"
  },
  "settings": {
    "title": "设置",
    "language": "界面语言",
    "languageZh": "简体中文",
    "languageEn": "English",
    "languageHelp": "切换后立即生效，会话内不刷新页面",
    "apiPort": "API 端口",
    "apiToken": "API Token",
    "browserPath": "默认浏览器路径",
    "masterPassword": "主密码",
    "masterPasswordHelp": "启用后配置文件将以 AES-256-GCM 加密",
    "cascadeOffset": "窗口错位间距",
    "autoAttach": "启动时自动接管仍在运行的窗口",
    "keepInMenuBar": "在菜单栏常驻",
    "theme": "主题"
  },
  "platform": {
    "windows": "Windows",
    "mac": "macOS",
    "linux": "Linux",
    "android": "Android"
  },
  "proxy": {
    "none": "不使用代理",
    "http": "HTTP 代理",
    "https": "HTTPS 代理",
    "socks5": "SOCKS5 代理",
    "custom": "跟随系统代理",
    "check": "检测代理",
    "checking": "检测中…",
    "checkOk": "出口 IP：{ip}（{country} {city}）{ms}ms",
    "checkFailed": "代理检测失败：{error}",
    "username": "账号",
    "password": "密码",
    "host": "主机",
    "port": "端口"
  },
  "fingerprint": {
    "modeRandom": "随机（每次打开生成）",
    "modeCustom": "自定义",
    "modeReal": "真实机器指纹",
    "ua": "User-Agent",
    "canvas": "Canvas",
    "webgl": "WebGL",
    "audio": "音频",
    "webrtc": "WebRTC",
    "fonts": "字体",
    "timezone": "时区",
    "languages": "语言",
    "geo": "地理位置",
    "screen": "屏幕",
    "battery": "电池",
    "speech": "语音",
    "plugins": "插件",
    "media": "媒体设备"
  },
  "detect": {
    "title": "指纹一致性检测",
    "score": "一致性得分：{score}",
    "checkUa": "User-Agent",
    "checkUaValue": "UA 与预期一致",
    "checkPlatform": "平台",
    "checkPlatformValue": "platform 与 UA 一致",
    "checkLang": "语言",
    "checkLangValue": "navigator.language 与 UA 一致",
    "checkTz": "时区",
    "checkTzValue": "时区与 country 匹配",
    "checkScreen": "屏幕",
    "checkScreenValue": "screen.width/height 在合理范围",
    "checkDpr": "设备像素比",
    "checkDprValue": "devicePixelRatio 在常见值",
    "checkWebglVendor": "WebGL Vendor",
    "checkWebglVendorValue": "WebGL vendor 与显卡匹配",
    "checkWebglRenderer": "WebGL Renderer",
    "checkWebglRendererValue": "renderer 与 vendor 一致",
    "checkFonts": "字体",
    "checkFontsValue": "字体表与平台一致",
    "checkWebdriver": "webdriver",
    "checkWebdriverValue": "navigator.webdriver === false",
    "checkCores": "CPU 核心数",
    "checkCoresValue": "hardwareConcurrency 合理",
    "checkMemory": "设备内存",
    "checkMemoryValue": "deviceMemory 合理",
    "checkPlugins": "插件",
    "checkPluginsValue": "plugins 数量符合预期",
    "checkLanguages": "navigator.languages",
    "checkLanguagesValue": "languages 数组与 locale 一致",
    "checkConnection": "网络连接",
    "checkConnectionValue": "navigator.connection 已伪装",
    "checkBattery": "电池",
    "checkBatteryValue": "Battery API 已拦截",
    "checkSpeech": "语音",
    "checkSpeechValue": "speechSynthesis.getVoices() 数量合理",
    "checkStorage": "存储",
    "checkStorageValue": "StorageManager.estimate 已伪装",
    "checkMedia": "媒体",
    "checkMediaValue": "enumerateDevices 已冻结",
    "checkNotification": "通知权限",
    "checkNotificationValue": "Notification.permission 状态",
    "checkPermissions": "权限",
    "checkPermissionsValue": "permissions.query 已拦截",
    "checkColorDepth": "色深",
    "checkColorDepthValue": "screen.colorDepth 合理",
    "checkWebgpu": "WebGPU",
    "checkWebgpuValue": "navigator.gpu 已隐藏或暴露",
    "checkTouch": "触控",
    "checkTouchValue": "maxTouchPoints 与平台一致",
    "checkPdfViewer": "PDF 查看器",
    "checkPdfViewerValue": "PDFViewerEnabled 合理",
    "checkProductSub": "productSub",
    "checkProductSubValue": "productSub === 20030107",
    "checkVendor": "navigator.vendor",
    "checkVendorValue": "vendor 字符串合理",
    "checkDoNotTrack": "DNT",
    "checkDoNotTrackValue": "doNotTrack 状态合理",
    "report": "查看详细报告",
    "allPass": "全部通过",
    "partiallyPass": "部分通过"
  },
  "group": {
    "default": "默认分组",
    "newGroup": "新建分组",
    "groupName": "分组名",
    "color": "颜色",
    "remark": "备注"
  },
  "profile": {
    "name": "名称",
    "remark": "备注",
    "tags": "标签",
    "group": "分组",
    "enabled": "启用",
    "platform": "平台",
    "country": "国家",
    "createdAt": "创建时间",
    "lastOpenedAt": "上次打开",
    "openCount": "打开次数",
    "seq": "序号",
    "id": "ID",
    "status": "状态",
    "statusRunning": "运行中",
    "statusStopped": "已停止"
  },
  "error": {
    "network": "网络错误",
    "timeout": "请求超时",
    "unknown": "未知错误",
    "noBrowser": "找不到可用的 Chromium 内核浏览器",
    "saveSettings": "保存设置失败"
  },
  "onboarding": {
    "welcome": "欢迎使用 Veil",
    "firstRun": "首次启动需要采集本机真实指纹作为随机指纹的种子（约 3 秒）"
  }
}
```

- [ ] **Step 4: JSON 合法性验证**

```bash
python3 -c "import json; d = json.load(open('/Users/alphaseven/Documents/Codex/nlviews/app/Web/locales/zh-CN.json')); print('zh-CN keys:', sum(1 for k in d if not k.startswith('_')))"
```

Expected: 数字 ≥ 15（即 nav/common/toast/dialog/button/settings/platform/proxy/fingerprint/detect/group/profile/error/onboarding 等组都存在）

- [ ] **Step 5: 添加 locales/.gitkeep 让目录提交**

```bash
touch /Users/alphaseven/Documents/Codex/nlviews/app/Web/locales/.gitkeep
ls -la /Users/alphaseven/Documents/Codex/nlviews/app/Web/locales/
```

Expected: `.gitkeep` 和 `zh-CN.json` 都存在。

- [ ] **Step 6: 提交**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
git add app/Web/locales/
git commit -m "feat(i18n): zh-CN 基线字典 (~150 keys，覆盖 nav/toast/dialog/settings/detect 等)"
```

---

### Task 4: 写 en-US.json（AI 翻译 + 自动校验）

**Files:**
- Create: `app/Web/locales/en-US.json`

**Interfaces:** 与 zh-CN.json 完全对齐（相同键集合，相同占位符）

- [ ] **Step 1: 准备 AI 翻译提示词**

把 `app/Web/locales/zh-CN.json` 内容喂给 AI 模型，提示词如下：

```
请把以下 JSON 字典翻译为英文（en-US），作为 macOS 指纹浏览器 Veil 的 UI 翻译。
严格规则：
1. 保持键结构（嵌套）完全一致
2. 保持所有 ICU 占位符原样：
   - {{var}} 简单插值
   - {n, plural, one {X} other {Y}} 复数（zh-CN 只有 other 分支；en-US 需要 one/other）
   - {gender, select, male {X} female {Y} other {Z}} select
   - # 在复数分支内表示当前数字
3. _meta 段保留（name 改为 "English"，code 改为 "en-US"）
4. 平台枚举：Windows / macOS / Linux / Android（专有名词）
5. 错误消息简洁专业
6. 输出 JSON 合法（最后用 ```json ... ``` 包裹）
7. 不要翻译：UA、WebGL、CDP、JS、Swift 等专有名词
8. 不要修改任何键名

下面是字典：
[粘贴 zh-CN.json]
```

- [ ] **Step 2: 校验键集合一致**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
python3 <<'PY'
import json, sys
def flat(d, prefix=''):
    out = []
    for k, v in d.items():
        if k.startswith('_'): continue
        key = f"{prefix}.{k}" if prefix else k
        if isinstance(v, dict):
            out.extend(flat(v, key))
        else:
            out.append((key, v))
    return out

zh = flat(json.load(open('app/Web/locales/zh-CN.json')))
en = flat(json.load(open('app/Web/locales/en-US.json')))

zh_set = {k for k, _ in zh}
en_set = {k for k, _ in en}
missing_in_en = zh_set - en_set
extra_in_en = en_set - zh_set
print(f"zh keys: {len(zh_set)}, en keys: {len(en_set)}")
print(f"missing in en: {sorted(missing_in_en)}")
print(f"extra in en: {sorted(extra_in_en)}")
sys.exit(1 if missing_in_en or extra_in_en else 0)
PY
```

Expected: 两边键数相等，`missing` 和 `extra` 都为空。Exit 0。

- [ ] **Step 3: 校验占位符一致**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
python3 <<'PY'
import json, re, sys
def flat(d, prefix=''):
    out = []
    for k, v in d.items():
        if k.startswith('_'): continue
        key = f"{prefix}.{k}" if prefix else k
        if isinstance(v, dict):
            out.extend(flat(v, key))
        else:
            out.append((key, v))
    return out

def placeholders(s):
    # 提取 {{var}}, {var}, {var, plural|select, ...} 里的 var 名
    out = set()
    out.update(re.findall(r'\{\{(\w+)\}\}', s))
    out.update(re.findall(r'\{(\w+)(?:\s*,)', s))
    return out

zh = dict(flat(json.load(open('app/Web/locales/zh-CN.json'))))
en = dict(flat(json.load(open('app/Web/locales/en-US.json'))))

mismatches = []
for k in zh:
    if k in en:
        z = placeholders(zh[k])
        e = placeholders(en[k])
        if z != e:
            mismatches.append((k, z, e))
print(f"占位符不一致的键: {len(mismatches)}")
for k, z, e in mismatches[:10]:
    print(f"  {k}: zh={z} en={e}")
sys.exit(1 if mismatches else 0)
PY
```

Expected: `占位符不一致的键: 0`，Exit 0。

- [ ] **Step 4: JSON 合法性验证**

```bash
python3 -m json.tool /Users/alphaseven/Documents/Codex/nlviews/app/Web/locales/en-US.json > /dev/null && echo "en-US.json OK"
```

Expected: `en-US.json OK`

- [ ] **Step 5: 人工抽检 20%**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
python3 <<'PY'
import json
def flat(d, prefix=''):
    out = []
    for k, v in d.items():
        if k.startswith('_'): continue
        key = f"{prefix}.{k}" if prefix else k
        if isinstance(v, dict):
            out.extend(flat(v, key))
        else:
            out.append((key, v))
    return out

en = flat(json.load(open('app/Web/locales/en-US.json')))
total = len(en)
step = 5  # 每 5 条抽 1 条 = 20%
samples = [(i, en[i]) for i in range(0, total, step)]
print(f"共 {total} 条键，每 {step} 条抽 1 条 = {len(samples)} 条待人工抽检")
print("--- 抽检清单 ---")
for i, (k, v) in samples:
    print(f"  [{i:3d}] {k}: {v}")
PY
```

人工审阅输出，检查：
- 术语一致（"窗口" 全翻译为 "Profile" 或 "Window"，不混用）
- 长度爆炸（短中文翻译为英文后不应 > 3x）
- 品牌名（Veil / Chrome / CDP）保留
- 占位符未改动

发现问题 → 手动修改 `app/Web/locales/en-US.json` → 重新跑 Step 2/3 校验。

- [ ] **Step 6: 提交**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
git add app/Web/locales/en-US.json
git commit -m "feat(i18n): en-US 字典 (AI 生成 + 占位符/键集合校验通过 + 20% 抽检)"
```

---

### Task 5: 修改 index.html（引入 i18n + 标记 data-i18n）

**Files:**
- Modify: `app/Web/index.html`

**Interfaces (this task uses):**
- `i18n.init({locale})` — 来自 Task 2
- `i18n.applyToDOM()` — 来自 Task 2

- [ ] **Step 1: 在 head 加 lang 属性 + i18n.js 引用**

修改 `app/Web/index.html`，在 `<title>` 后插入：

```html
<meta name="i18n-locale" content="__VEIL_LOCALE__">
```

说明：实际值由 Swift 端通过 String 替换注入；占位符 `__VEIL_LOCALE__` 暂时保留（Task 11 处理）。

在 `</head>` 前加：

```html
<script src="i18n.js"></script>
```

- [ ] **Step 2: 添加 init 调用**

在 `<div id="app" class="app hidden">` 之前插入：

```html
<script>
(function(){
  var init = function(){
    if (!window.i18n) return;
    var locale = (document.querySelector('meta[name="i18n-locale"]')||{}).content || 'zh-CN';
    if (locale === '__VEIL_LOCALE__') locale = 'zh-CN';
    i18n.init({ locale: locale, fallback: 'zh-CN' }).then(function(){
      i18n.applyToDOM();
      document.documentElement.setAttribute('lang', locale);
      document.getElementById('boot') && document.getElementById('boot').classList.add('gone');
    }).catch(function(e){
      console.error('[i18n] init failed', e);
    });
  };
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
})();
</script>
```

- [ ] **Step 3: 标记静态可见文本为 data-i18n**

读取 `app/Web/index.html` 全文，识别硬编码中文文本节点，按以下规则替换：

| 原 HTML | 修改为 |
|---|---|
| `<span>窗口</span>` | `<span data-i18n="nav.profiles">窗口</span>` |
| `<input placeholder="搜索…">` | `<input data-i18n-placeholder="nav.searchPlaceholder" placeholder="搜索…">` |
| `<button title="关闭">×</button>` | `<button data-i18n-attr="title:common.close" title="关闭">×</button>` |

具体清单（已知需改的）：
- 侧边栏 4 个 nav-item（profiles/running/groups/templates）
- 侧边栏 3 个（detect/api/settings）
- 顶部搜索框 placeholder
- 状态栏统计文字
- host-card 标题与字段名（Chrome / GPU / 时区）

- [ ] **Step 4: 验证修改**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
echo "=== 修改后剩余中文（应 < 10 处，仅注释/属性） ==="
grep -nE "[\xe4-\xe9]" app/Web/index.html | wc -l
echo "=== data-i18n 属性数 ==="
grep -cE "data-i18n" app/Web/index.html
```

Expected: 中文数减少（从 38 → 接近 0），data-i18n 数 > 10。

- [ ] **Step 5: 浏览器实测（手动）**

```bash
# 启动一次只是为了确认无语法错；正式启动在 Phase 4
python3 -c "from html.parser import HTMLParser; HTMLParser().feed(open('/Users/alphaseven/Documents/Codex/nlviews/app/Web/index.html').read()); print('HTML parse OK')"
```

Expected: `HTML parse OK`

- [ ] **Step 6: 提交**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
git add app/Web/index.html
git commit -m "feat(i18n): index.html 引入 i18n.js + data-i18n 属性"
```

---

### Task 6: 修改 detect.html（35+ 项测试名翻译）

**Files:**
- Modify: `app/Web/detect.html`

**Interfaces:** 复用 i18n API（同 Task 5）

- [ ] **Step 1: 与 Task 5 Step 1/2 相同的方式注入 i18n**

在 `<head>` 加 `<script src="i18n.js"></script>` 和 init 调用（同 Task 5 Step 1/2 模式）。

- [ ] **Step 2: 标记所有 35+ 测试项为 data-i18n**

读取 `app/Web/detect.html`，找出每个测试项的标题 span，按 `detect.check*` key 标记。例：

```html
<!-- 修改前 -->
<span class="test-name">User-Agent</span>
<!-- 修改后 -->
<span class="test-name" data-i18n="detect.checkUa">User-Agent</span>
```

35 项清单见 `~/Documents/Obisidian/LocalProject/Project/nlviews 任务/20260912_nlviews_i18n.md` §6.2 量化指标。

- [ ] **Step 3: 验证修改**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
echo "=== detect.html data-i18n 数 ==="
grep -cE "data-i18n" app/Web/detect.html
echo "=== 检测项数 ==="
grep -cE "check[A-Z]" app/Web/detect.html
```

Expected: data-i18n 数 ≈ 检测项数（35+）。

- [ ] **Step 4: 提交**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
git add app/Web/detect.html
git commit -m "feat(i18n): detect.html 35+ 项测试名走 i18n"
```

---

### Task 7: 修改 app.js（所有用户可见字符串字典化）

**Files:**
- Modify: `app/Web/app.js`

**Interfaces:** `i18n.t(key, params)`、`i18n.applyToDOM()`

- [ ] **Step 1: 扫描需改的中文字符串**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
# 提取 app.js 中所有中文字符串，按行号列出
grep -nE "[\xe4-\xe9]" app/Web/app.js | wc -l
echo "=== 前 20 处示例 ==="
grep -nE "[\xe4-\xe9]" app/Web/app.js | head -20
```

记录行号与上下文，为 Step 2 准备。

- [ ] **Step 2: 创建键映射参考表（人工）**

打开 `/tmp/zh-strings-appjs.txt`（Task 3 Step 1 产出），逐条对照 zh-CN.json 已有的 key：
- 已存在 → 用现有 key
- 不存在 → 新增到 zh-CN.json 的合适分组（并补 en-US.json 对应翻译）

维护 `/tmp/i18n-key-map.md`，记录"原字符串 → i18n key"映射。

- [ ] **Step 3: 批量替换（按区域）**

按以下顺序替换（每改完一段跑一次 `node --check`）：

**区域 1：toast 消息**
```javascript
// 修改前
toast('已保存');
// 修改后
toast(i18n.t('toast.saved'));

// 修改前
toast(`已导入 ${n} 个窗口`);
// 修改后
toast(i18n.t('toast.imported', {n: n}));
```

**区域 2：对话框 / confirm**
```javascript
// 修改前
if (!confirm(`确定要删除「${name}」吗？`)) return;
// 修改后
if (!confirm(i18n.t('dialog.deleteConfirm', {name: name}))) return;
```

**区域 3：动态插入的 HTML / list / table**
```javascript
// 修改前
el.innerHTML = `<button>${name} - 克隆</button>`;
// 修改后
el.innerHTML = `<button data-i18n="common.duplicate">${name}</button>`;
```

**区域 4：title / placeholder 属性**
```javascript
// 修改前
btn.title = '关闭';
// 修改后
btn.title = i18n.t('common.close');
```

- [ ] **Step 4: 跑 Node 语法检查**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
node --check app/Web/app.js && echo "SYNTAX OK"
```

Expected: `SYNTAX OK`

- [ ] **Step 5: 残留中文扫描**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
echo "=== 剩余中文字符串行数（应 < 30，主要是注释/正则/键名） ==="
grep -nE "[\xe4-\xe9]" app/Web/app.js | wc -l
echo "=== 列出剩余 ==="
grep -nE "[\xe4-\xe9]" app/Web/app.js | head -30
```

人工审阅每条，确认都是注释 / 正则 / 键名 / URL / 文件名（如 `~/Library/Application Support/Veil/` 之类没有中文），不应再有用户可见字符串字面量。

- [ ] **Step 6: 提交**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
git add app/Web/app.js
git commit -m "feat(i18n): app.js 用户可见字符串字典化 (~2149 -> 剩余 < 30 注释)"
```

---

### Task 8: 修改 Swift StaticFiles（支持 locales/ 子目录）

**Files:**
- Modify: `app/Sources/StaticFiles.swift`

**Interfaces:** 公开 `serve(_:query:)` 不变，内部支持子目录

- [ ] **Step 1: 读取现有实现**

```bash
cat /Users/alphaseven/Documents/Codex/nlviews/app/Sources/StaticFiles.swift
```

记下当前的 `staticExts` / `root` 路径 / `serve` 函数签名。

- [ ] **Step 2: 扩展 serve 函数支持 locales/ 子目录**

修改 `app/Sources/StaticFiles.swift`，在 `serve` 入口处加：

```swift
// 关键路径：locales/<lang>.json
if path.hasPrefix("locales/") {
    let sub = String(path.dropFirst("locales/".count))
    return serveFile(name: sub, in: "Web/locales")
}
```

（具体 bundle 路径查找逻辑沿用项目现有约定；如果有 Bundle.module / Bundle.main URL 模式，照搬。）

- [ ] **Step 3: 编译验证**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
swiftc -sdk "$(xcrun --show-sdk-path)" -target "$(uname -m)-apple-macos13.0" \
  -framework AppKit -framework WebKit -framework Network -framework CryptoKit \
  -framework Security -framework UniformTypeIdentifiers \
  -parse-as-library \
  -o /tmp/veil-static-check \
  $(find app/Sources -name '*.swift') 2>&1 | head -30
echo "exit:$?"
```

Expected: `exit:0`，无报错。

- [ ] **Step 4: 提交**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
git add app/Sources/StaticFiles.swift
git commit -m "feat(i18n): StaticFiles 支持 locales/ 子目录"
```

---

### Task 9: 修改 Swift LocalAPI（新增 2 个端点）

**Files:**
- Modify: `app/Sources/LocalAPI.swift`

**Interfaces (this task adds):**
- `GET /__veil/locale` 返回 `{"data":{"locale":"<lang>"}}`
- `GET /__veil/locales/<lang>` 转发到 StaticFiles（locales 子目录）

- [ ] **Step 1: 找到现有 /__veil/* 路由分支**

```bash
grep -nE '__veil/' /Users/alphaseven/Documents/Codex/nlviews/app/Sources/LocalAPI.swift
```

记下分支位置。

- [ ] **Step 2: 在 /__veil/* 分支加 2 个 case**

修改 `route(_:)` 函数，在已有的 `if path.hasPrefix("/__veil/")` 内追加：

```swift
if sub == "locale" {
    let s = Store.shared.settings()
    return ok(["locale": s.locale])
}
if sub.hasPrefix("locales/") {
    let name = String(sub.dropFirst("locales/".count))
    return StaticFiles.serveFile(name: name, in: "Web/locales")
}
```

（如果 `StaticFiles.serveFile(name:in:)` 不存在，复用 Task 8 抽出来的内部函数。）

- [ ] **Step 3: 编译验证**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
swiftc -sdk "$(xcrun --show-sdk-path)" -target "$(uname -m)-apple-macos13.0" \
  -framework AppKit -framework WebKit -framework Network -framework CryptoKit \
  -framework Security -framework UniformTypeIdentifiers \
  -parse-as-library \
  -o /tmp/veil-api-check \
  $(find app/Sources -name '*.swift') 2>&1 | head -30
echo "exit:$?"
```

Expected: `exit:0`。

- [ ] **Step 4: 提交**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
git add app/Sources/LocalAPI.swift
git commit -m "feat(i18n): LocalAPI 新增 /__veil/locale 与 /__veil/locales/<lang> 端点"
```

---

### Task 10: 修改 Swift AppDelegate（注入 locale + 监听设置变化）

**Files:**
- Modify: `app/Sources/AppDelegate.swift`

**Interfaces (this task uses):**
- `Store.shared.settings()` — 已有
- `webView.evaluateJavaScript(_:)` — 已有（WKWebView）
- `Bridge.shared` 调用 `saveSettings` 后触发 JS

- [ ] **Step 1: 找到 webView 初始化位置**

```bash
grep -nE "webView|WKWebView|loadFileURL|loadHTMLString" /Users/alphaseven/Documents/Codex/nlviews/app/Sources/AppDelegate.swift | head -10
```

记下 webView 创建的位置。

- [ ] **Step 2: webView 加载完后注入 __veilLocale**

在 webView 加载完成回调（如 `didFinish` 或 navigation delegate）里加：

```swift
let locale = Store.shared.settings().locale
let js = "window.__veilLocale = \"\(locale)\";"
webView?.evaluateJavaScript(js)
```

（如果现有 init 流程是异步的，确保 inject 时机正确。）

- [ ] **Step 3: 在 saveSettings 后桥接 JS 切换语言**

找到 AppDelegate 内对 settings 变更的监听点（如果有 KVO/NotificationCenter），或修改 `Bridge.saveSettings` 实现，在 settings 持久化后追加：

```swift
let locale = s.locale
let js = "if(window.i18n){i18n.setLocale('\(locale)').then(function(){i18n.applyToDOM();});}"
webView?.evaluateJavaScript(js)
```

- [ ] **Step 4: 编译验证**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
swiftc -sdk "$(xcrun --show-sdk-path)" -target "$(uname -m)-apple-macos13.0" \
  -framework AppKit -framework WebKit -framework Network -framework CryptoKit \
  -framework Security -framework UniformTypeIdentifiers \
  -parse-as-library \
  -o /tmp/veil-app-check \
  $(find app/Sources -name '*.swift') 2>&1 | head -30
echo "exit:$?"
```

Expected: `exit:0`。

- [ ] **Step 5: 提交**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
git add app/Sources/AppDelegate.swift
git commit -m "feat(i18n): AppDelegate 注入 __veilLocale + 监听设置切换语言"
```

---

### Task 11: 设置面板加语言选择 UI

**Files:**
- Modify: `app/Web/index.html`（settings 区域追加 select）
- Modify: `app/Web/app.js`（保存 settings 时通知 Swift）

**Interfaces:** 复用 `i18n.t()` + `Bridge.saveSettings`

- [ ] **Step 1: 在 settings 视图加 select 控件**

找到 `app/Web/index.html` 中 settings 相关区域（grep `<select` 或搜索"语言"/"language"），插入：

```html
<div class="form-row">
  <label data-i18n="settings.language">界面语言</label>
  <select id="settingLocale">
    <option value="zh-CN" data-i18n="settings.languageZh">简体中文</option>
    <option value="en-US" data-i18n="settings.languageEn">English</option>
  </select>
  <small data-i18n="settings.languageHelp">切换后立即生效</small>
</div>
```

- [ ] **Step 2: 在 app.js settings 保存逻辑里携带 locale**

找到 settings 保存函数（grep `saveSettings`），确保持久化的对象包含 `locale`：

```javascript
// 修改前
const s = collectSettings();
// 修改后
const s = collectSettings();
s.locale = document.getElementById('settingLocale').value;
```

- [ ] **Step 3: 验证**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
node --check app/Web/app.js && echo "JS OK"
grep -nE "settingLocale" app/Web/app.js | head -3
grep -nE "settingLocale" app/Web/index.html
```

Expected: 全部有输出。

- [ ] **Step 4: 提交**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
git add app/Web/index.html app/Web/app.js
git commit -m "feat(i18n): 设置面板语言选择 + locale 持久化"
```

---

### Task 12: 端到端构建 + 浏览器验证

**Files:**
- Build: 全部
- Run: `open dist/stage/Veil.app`

**Interfaces:** 全栈

- [ ] **Step 1: 一键构建**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
bash ./build.sh 2>&1 | tail -30
echo "exit:$?"
ls -la dist/stage/Veil.app/Contents/MacOS/Veil dist/Veil-1.0.0.pkg
```

Expected: exit 0，二进制和 pkg 都存在。

- [ ] **Step 2: 启动并等就绪**

```bash
open /Users/alphaseven/Documents/Codex/nlviews/dist/stage/Veil.app
sleep 8
ps aux | grep "Veil.app/Contents/MacOS/Veil" | grep -v grep | head -1
```

Expected: 1 个进程输出。

- [ ] **Step 3: 验证 locale 端点**

```bash
curl -s http://127.0.0.1:54345/__veil/locale
echo
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:54345/__veil/locales/zh-CN.json
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:54345/__veil/locales/en-US.json
```

Expected: `{"data":{"locale":"zh-CN"}}` + 两个 200。

- [ ] **Step 4: 浏览器访问主界面（中文默认）**

```bash
curl -s http://127.0.0.1:54345/ | grep -oE "data-i18n=\"[^\"]+\"" | wc -l
```

Expected: > 10。

- [ ] **Step 5: 通过 saveSettings 切换英文**

```bash
curl -s -X POST http://127.0.0.1:54345/saveSettings \
  -H 'Content-Type: application/json' \
  -d '{"settings":{"locale":"en-US"}}'
```

Expected: `{"success":true}` 之类。

- [ ] **Step 6: 验证 locale 已切换**

```bash
curl -s http://127.0.0.1:54345/__veil/locale
```

Expected: `{"data":{"locale":"en-US"}}`

- [ ] **Step 7: 退出 Veil**

```bash
osascript -e 'tell application "Veil" to quit'
sleep 2
pkill -f "Veil.app/Contents/MacOS/Veil" 2>&1
ps aux | grep "Veil.app/Contents/MacOS/Veil" | grep -v grep | wc -l
```

Expected: 0 残留。

- [ ] **Step 8: 重启验证持久化**

```bash
open /Users/alphaseven/Documents/Codex/nlviews/dist/stage/Veil.app
sleep 6
curl -s http://127.0.0.1:54345/__veil/locale
```

Expected: `{"data":{"locale":"en-US"}}`（持久化生效）

- [ ] **Step 9: 完整 grep 残留检查**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
echo "=== app.js 残留中文行数（应 < 30，注释/正则） ==="
grep -cE "[\xe4-\xe9]" app/Web/app.js
echo "=== index.html 残留 ==="
grep -cE "[\xe4-\xe9]" app/Web/index.html
echo "=== detect.html 残留 ==="
grep -cE "[\xe4-\xe9]" app/Web/detect.html
```

Expected: 全部显著降低（从 2149 / 38 / 69 → 各 < 30）。

- [ ] **Step 10: 最终提交（如有微调）**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
git status --short
# 如果有未提交微调：
git add -A
git commit -m "fix(i18n): Phase 4 验证发现的微调"
```

- [ ] **Step 11: 推送 GitHub**

```bash
cd /Users/alphaseven/Documents/Codex/nlviews
git push origin main 2>&1 | tail -5
git log --oneline -15
```

Expected: 推送成功，git log 列出 14 个新 commit。

---

### Task 13: 更新 Obsidian 任务索引

**Files:**
- Modify: `~/Documents/Obisidian/LocalProject/Project/nlviews 任务/_README.md`
- Modify: `~/Documents/Obisidian/LocalProject/Project/nlviews 任务/20260912_nlviews_i18n.md` §1.1 / §1.2

- [ ] **Step 1: 把任务状态改为 ✅ 完成**

修改 `_README.md` 中的状态行：

```diff
-| 2026-09-12 | 🟡 Phase 1 Brainstorm | 国际化 UI（zh-CN / en-US） | [链接](20260912_nlviews_i18n.md) |
+| 2026-09-12 | ✅ 已完成 | 国际化 UI（zh-CN / en-US） | [链接](20260912_nlviews_i18n.md) |
```

- [ ] **Step 2: 更新任务文件的 Phase 状态**

修改 `20260912_nlviews_i18n.md` §1.1，把所有 Phase 标 ✅。

- [ ] **Step 3: 写变更日志到任务文件末尾**

追加到任务文件：

```markdown
## 变更日志

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-09-12 | 1.0.0 | 完成 zh-CN/en-US 双语支持；新建 i18n.js 运行时 + 2 个 JSON 字典 + 修改 5 个文件；端到端验证通过 |
```

---

## Self-Review Checklist（完成后跑一次）

| 检查项 | 验证方式 |
|---|---|
| 字典键数 zh == en | Task 4 Step 2 脚本 |
| 占位符一致 | Task 4 Step 3 脚本 |
| JSON 合法 | `python3 -m json.tool < *.json` |
| JS 语法 | `node --check i18n.js / app.js` |
| Swift 编译 | `swiftc ... -o /tmp/check ...` exit 0 |
| 端到端 | Task 12 完整跑一遍 |
| 持久化 | 切换英文 → 重启 → 仍英文 |
| 残留中文 | grep 数显著下降 |
| GitHub 推送 | `git push origin main` 成功 |
| Obsidian 索引 | _README.md 已更新为 ✅ |
